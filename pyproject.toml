[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "boundarykit"
requires-python = ">=3.11"
version = "0.1.0"
description = "Build and export OpenStreetMap relation geometries"
dependencies = []

[project.scripts]
boundarykit = "boundarykit.cli:main"

[dependency-groups]
dev = [
    "black==26.5.1",
    "isort==8.0.1",
    "pylint==4.0.8",
]

[tool.hatch.build.targets.wheel]
packages = ["src/boundarykit"]

[tool.black]
line-length = 80
target-version = ["py311"]
include = '\.pyi?$'

[tool.isort]
profile = "google"
line_length = 80
src_paths = ["src", "tests"]
